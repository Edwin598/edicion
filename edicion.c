#include <stdio.h>
#include <fcntl.h>
int asignacionNull(char *[]);
int wrOut(char *[], int *);
int main(void){
	int count1,count2,cBytes=0,comparadorBytes;
	char bufferOut[1000], bufferInt[cBytes];
	asignacionNull();
};
int asignacionNull(char *array[]){
	for(int i=0;i<=1000;i++){
		array[i]='\0';
	};
};
int wrOut(char *array1, int count){

};
